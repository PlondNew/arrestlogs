specVersion: "v1beta"
serviceId: "arrestlogs"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "arrestlogs-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
